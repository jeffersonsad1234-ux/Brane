{
  "name": "brane",
  "version": "1.0.0",
  "author": "Branpy",
  "private": true,
  "main": "electron/main.js",
  "description": "Monorepo root for Cloudflare Pages. Option A (recommended): Root directory = repo root, Build command = npm run build, Build output = frontend/build. Option B: Root directory = frontend, Build command = npm run build, Build output = build.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install": "npm install --prefix ./frontend --legacy-peer-deps",
    "build": "npm install --prefix ./frontend --legacy-peer-deps && npm run build --prefix ./frontend && npm run build:engine",
    "build:engine": "cd branpy-game-engine && npm install --legacy-peer-deps && npm run build && node -e \"var fs=require('fs'),p=require('path');var s='dist',d='../frontend/build/engine';if(!fs.existsSync(s)){console.error('engine dist missing');process.exit(1)};function cpR(src,dst){fs.mkdirSync(dst,{recursive:true});fs.readdirSync(src).forEach(function(f){var sp=p.join(src,f),dp=p.join(dst,f);fs.statSync(sp).isDirectory()?cpR(sp,dp):fs.copyFileSync(sp,dp)})};if(fs.existsSync(d)){fs.rmSync(d,{recursive:true,force:true})}cpR(s,d);console.log('Engine copied to '+d);\"",
    "start:fe": "npm start --prefix ./frontend",
    "dev": "npm start --prefix ./frontend",
    "electron:dev": "concurrently -k \"npm start --prefix ./frontend\" \"wait-on http://localhost:3001 && cross-env NODE_ENV=development electron .\"",
    "electron:build:react": "cross-env PUBLIC_URL=. npm run build --prefix ./frontend",
    "electron:build": "npm run electron:build:react && electron-builder --win --config electron-builder.yml",
    "electron:start": "electron ."
  },
  "dependencies": {
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "electron": "^33.4.11",
    "electron-builder": "^25.1.8",
    "wait-on": "^8.0.5"
  }
}
