# Brane Agent — Configuration
# Copy this to .env and fill in your values

# Admin password for /brane-agent login
AGENT_PASSWORD=your-secure-password-here

# Server port (default 3200, Railway sets via $PORT)
PORT=3200

# ── AI Provider ──
# Options: openrouter, openai, ollama (default: openrouter)
AI_PROVIDER=openrouter

# Default model (OpenRouter ID or OpenAI model name)
AI_MODEL=anthropic/claude-3.5-sonnet

# OpenRouter API key (required if AI_PROVIDER=openrouter)
OPENROUTER_API_KEY=sk-or-your-key-here

# OpenAI API key (required if AI_PROVIDER=openai)
OPENAI_API_KEY=sk-your-key-here

# Local model URL (required if AI_PROVIDER=ollama)
LOCAL_MODEL_URL=http://localhost:11434/api/generate

# Project root path (absolute path on the server)
# On Railway: /app
# On Render: /opt/render/project/src
PROJECT_ROOT=/app

# Allowed commands (comma-separated, empty = allow all)
ALLOWED_COMMANDS=npm run build,npm start,npm test,git status,git diff,git log --oneline -10,dir,ls

# Production domains (for CORS)
# ORIGINS=https://branded.page.br,https://www.branded.page.br

# ── Available Models via OpenRouter ──
# anthropic/claude-3.5-sonnet  → Powerful, medium cost
# anthropic/claude-3-haiku     → Fast, cheap
# openai/gpt-4o                → Powerful, medium cost
# openai/gpt-4o-mini           → Cheap, good for simple tasks
# google/gemini-2.0-flash-001  → Fast, cheap
# deepseek/deepseek-coder      → Cheap, code-focused
#
# ── Available Models via OpenAI ──
# gpt-4o                       → Powerful
# gpt-4o-mini                  → Cheap
#
# ── Available Models via Ollama (local) ──
# llama3.1                     → Free, local
# codellama                    → Free, local, code-focused
