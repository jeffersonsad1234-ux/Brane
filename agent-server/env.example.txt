# Brane Agent - Configuration
# Copy this to .env and fill in your values

# Admin password for /brane-agent login
AGENT_PASSWORD=your-secure-password-here

# Server port (default 3200, Railway sets via $PORT)
PORT=3200

# AI Provider: "openai" or "local"
AI_PROVIDER=openai

# OpenAI API key (required if AI_PROVIDER=openai)
OPENAI_API_KEY=sk-your-key-here

# Local model URL (required if AI_PROVIDER=local)
LOCAL_MODEL_URL=http://localhost:11434/api/generate

# Project root path (absolute path on the server)
# On Railway: /app (the deploy root)
# On Render: /opt/render/project/src
PROJECT_ROOT=/app

# Allowed commands (comma-separated, empty = allow all)
ALLOWED_COMMANDS=npm run build,npm start,npm test,git status,git diff,git log --oneline -10,dir,ls

# Production domains (for CORS)
# ORIGINS=https://branded.page.br,https://www.branded.page.br
